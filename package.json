{
  "name": "asc-tiktok-clone",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon -r dotenv/config --exec ts-node backend/index.ts",
    "client": "npm start --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^7.1.0",
    "dotenv": "^16.0.0",
    "nodemon": "^2.0.16",
    "ts-node": "^10.7.0"
  }
}
